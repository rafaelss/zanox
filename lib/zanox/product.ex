defmodule Product do
  defstruct [:id, :name, :modified, :program, :price, :currency, :tracking_links,
    :description, :manufacturer, :ean, :images, :merchant_category,
    :merchant_product_id]
end
